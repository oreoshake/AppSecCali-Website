<div class="masthead">
  <ul class="nav nav-pills pull-right">
    <li class="active"><a href="#">Home</a></li>
    <li><a href="<%= speakers_path %>">Confirmed Speakers</a></li>
    <li><a href="<%= cfp_speakers_path %>">Call For Papers</a></li>
    <li><a href="<%= contact_path %>">Contact</a></li>
    <li><a href="/CodeOfConduct">Code of Conduct</a></li>
  </ul>
  <h3 class="muted">AppSec California</h3>
</div>

<hr>

<%= render 'shared/jumbotron' %>

<hr>

<h4>Confirmed Speakers <a name="<%= speakers_path %>">(cont'd).</a></h4>
<div class="row-fluid marketing pagination-centered">
  <% @speakers.where(:confirmed => true).sample(4).each_with_index do |speaker, index| %>
    <%= render :partial => 'row', :locals => {:speaker => speaker} %>
  <% end %>

</div>

<hr>

<%= render 'shared/location' %>

<hr>

<%= render 'shared/cfp' %>
