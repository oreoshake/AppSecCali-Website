
    <div class="span3">
      <p><%= link_to image_tag(speaker.image_url, :height => 73, :width => 73), (speaker.slug == 'jim-manico' ? "speakers/#{speaker.id}" : speaker) %></p>
      <p><strong><%= speaker.name%></strong></p>
    </div>
