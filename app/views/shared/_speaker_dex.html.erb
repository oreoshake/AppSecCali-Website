<h4>Confirmed Speakers <a name="speakers" href="<%= speakers_path %>">(cont'd).</a></h4>

<div class="row-fluid marketing pagination-centered">
  <% Speaker.all.where(:confirmed => true).sample(4).each_with_index do |speaker, index| %>
    <%= render :partial => 'row', :locals => {:speaker => speaker} %>
  <% end %>
</div>